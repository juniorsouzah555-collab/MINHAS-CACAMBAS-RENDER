import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.relampago.motorista',
  appName: 'Portal Motorista',
  webDir: 'dist'
};

export default config;
